# Config Module

Central configuration loader for SAIOS runtime paths and feature flags.

## Interface

- `SaiosConfig` — resolved paths and channel toggles
- `SaiosConfigLoader` — `load()` returns `SaiosConfig`

## Status

Skeleton v1.0 — loader not implemented. No `.env` reads in this module yet.
