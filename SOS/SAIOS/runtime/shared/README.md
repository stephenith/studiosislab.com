# Shared Module

Common types used across all SAIOS runtime modules.

## Exports

- Priorities (`P0`–`P3`)
- Job and worker status enums
- ID type aliases (`JobId`, `WorkerId`, `PlanId`)
- `IsoTimestamp`, `Result<T>`

## Status

Skeleton v1.0 — no implementation.
